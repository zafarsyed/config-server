server:
  port: 8002
rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
cloud:
  stream:
    bindings:
      input:
        destination: source
     rabbit:
        bindings:
          output:
            producer:
              exchangeDurable: false
