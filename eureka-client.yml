server:
  port: 8002
rabbitmq:
  host:172.21.1.111
  port:5671
  username:guest
  password:guest
cloud:
  stream:
    bindings:
      output:
        destination:source
autoconfigure:
    exclude: org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
